//calculate the area of rect given its length & breath//
main()
{
   float len=4.5,bre=2.8,area;
   clrscr();
   area=len*bre;
   printf("\nLength=%f\n\nBreath=%f\n\nthe area of rect is=%f",len,bre,area);
   getch();
}
