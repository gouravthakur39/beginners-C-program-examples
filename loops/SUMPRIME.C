//program on prime number //
main()
{
   int j=1,i,rem,no,sum=0;
   clrscr();
  // printf("\n enter number");
 //  scanf("%d",&no);
   for(i=1;i<=20;i++)
   {
           no=i;
while(j<=no)
{
         rem=no%i;
	if(rem==0)
                  { c++; }
      j++;
}
if c==1 || c==2)
pri ntf("%d prime no",no);
}
   getch();
}